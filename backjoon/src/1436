#include <iostream>
#include <cmath>
using namespace std;

int main() {
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int a;
	int sum =0;
	cin >> a;
	int i = 666;
	string s;
	while(a != sum){
		s = to_string(i);
		if(s.find("666") != -1 ) sum += 1;
		i++;
		
	}
	cout << i-1;
}